#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=_utils.sh
source "${SCRIPT_DIR}/_utils.sh"

if [[ -f "$ROOT_DIR/alembic.ini" ]]; then
  activate_venv
  alembic upgrade head
else
  echo "No migrations defined; skipping." >&2
fi
